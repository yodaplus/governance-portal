version: '3.8'

services:
  db:
    image: mongo:4.4.14

  nginx:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile

  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
